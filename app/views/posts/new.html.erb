<h2>New Post </h2>

<%= form_for [current_user, @post] do |f| %>

  <div class="form-group">
		<%= f.label :avatar, 'User Photo', class:'col-sm-2 control-label' %>
  		<div class="col-sm-10">
    	<%= f.file_field :avatar  %>
  	</div>
	</div>
	

  <div class="form-group">
  		<div class="col-sm-10">
        discarption: <%= f.text_field :discarption %>
    	   </div>
	          </div>
	          <div class="col-sm-10">
        <%= f.submit "Submit" %>
      </div>
<% end %>